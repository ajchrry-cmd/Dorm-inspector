<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dorm Inspection System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            min-height: 100vh;
            padding: 10px;
            color: #e0e0e0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: #1e1e2e;
            border-radius: 15px;
            box-shadow: 0 15px 30px rgba(0,0,0,0.5);
            overflow: hidden;
            border: 1px solid #333;
        }

        .header {
            background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
            color: #e2e8f0;
            padding: 20px;
            text-align: center;
            border-bottom: 2px solid #4a5568;
        }

        .header h1 {
            font-size: 1.8em;
            margin-bottom: 8px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.5);
            color: #90cdf4;
        }

        .header p {
            font-size: 0.9em;
            opacity: 0.9;
            color: #cbd5e0;
        }

        .main-content {
            padding: 20px;
            background: #1a1a2e;
        }

        .nav-buttons {
            display: flex;
            gap: 8px;
            margin-bottom: 25px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .nav-btn {
            padding: 12px 18px;
            background: linear-gradient(145deg, #4a5568, #2d3748);
            color: #e2e8f0;
            border: none;
            border-radius: 20px;
            font-size: 0.75em;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.4s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
            border: 1px solid #4a5568;
        }

        .nav-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(74, 85, 104, 0.6);
            background: linear-gradient(145deg, #68d391, #48bb78);
            color: #1a202c;
        }

        .nav-btn.active {
            background: linear-gradient(145deg, #3182ce, #2c5282);
            box-shadow: 0 4px 15px rgba(49, 130, 206, 0.5);
            color: #e2e8f0;
        }

        .section {
            display: none;
        }

        .section.active {
            display: block;
        }

        .room-mode-toggle {
            background: linear-gradient(135deg, #2d3748, #1a202c);
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            border: 2px solid #3182ce;
            box-shadow: 0 2px 8px rgba(49, 130, 206, 0.2);
        }

        .toggle-content {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .toggle-label {
            font-weight: 700;
            color: #90cdf4;
            font-size: 0.9em;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            padding: 8px 12px;
            border-radius: 15px;
            transition: all 0.3s ease;
            background: rgba(74, 85, 104, 0.3);
            border: 1px solid #4a5568;
        }

        .radio-option:hover {
            background: rgba(74, 85, 104, 0.5);
            transform: translateY(-1px);
        }

        .radio-option input[type="radio"] {
            width: 16px;
            height: 16px;
            margin: 0;
            cursor: pointer;
            accent-color: #3182ce;
        }

        .radio-option span {
            font-weight: 600;
            color: #cbd5e0;
            font-size: 0.85em;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr;
            gap: 15px;
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 6px;
            font-weight: 700;
            color: #e2e8f0;
            font-size: 0.9em;
        }

        input, select, textarea {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #4a5568;
            border-radius: 10px;
            font-size: 14px;
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #2d3748, #1a202c);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
            font-weight: 600;
            color: #e2e8f0;
        }

        input::placeholder, textarea::placeholder {
            color: #a0aec0;
        }

        input:focus, select:focus, textarea:focus {
            outline: none;
            border-color: #3182ce;
            box-shadow: 0 0 0 3px rgba(49, 130, 206, 0.3), 0 4px 15px rgba(49, 130, 206, 0.3);
            transform: translateY(-1px);
            background: linear-gradient(135deg, #1a202c, #2d3748);
        }

        select option {
            background: #2d3748;
            color: #e2e8f0;
        }

        .demerits-section {
            background: linear-gradient(135deg, #2d3748, #1a202c);
            padding: 20px;
            border-radius: 15px;
            margin: 20px 0;
            box-shadow: inset 0 2px 10px rgba(0,0,0,0.3);
            border: 1px solid #4a5568;
        }

        .demerits-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 10px;
        }

        .demerit-item {
            background: linear-gradient(135deg, #4a5568, #2d3748);
            padding: 15px;
            border-radius: 12px;
            border: 2px solid #3182ce;
            cursor: pointer;
            transition: all 0.4s ease;
            position: relative;
            min-height: 50px;
            display: flex;
            align-items: center;
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.3);
            transform-origin: center;
        }

        .demerit-item:hover {
            border-color: #90cdf4;
            transform: translateY(-2px) scale(1.01);
            box-shadow: 0 6px 15px rgba(144, 205, 244, 0.4);
        }

        .demerit-item.checked {
            border-color: #f56565;
            background: linear-gradient(135deg, #742a2a, #553c3c);
            padding-left: 50px;
            padding-right: 40px;
            box-shadow: 0 6px 15px rgba(245, 101, 101, 0.3);
        }

        .demerit-item.checked::before {
            content: '✓';
            position: absolute;
            left: 15px;
            font-size: 20px;
            color: #f56565;
            font-weight: bold;
        }

        .demerit-item.checked::after {
            content: '⚠️';
            position: absolute;
            right: 12px;
            font-size: 16px;
        }

        .demerit-item.auto-failure {
            border-color: #e53e3e;
            background: linear-gradient(135deg, #742a2a, #553c3c);
            box-shadow: 0 4px 12px rgba(229, 62, 62, 0.3);
        }

        .demerit-item.auto-failure:hover {
            border-color: #c53030;
            transform: translateY(-2px) scale(1.01);
            box-shadow: 0 8px 18px rgba(197, 48, 48, 0.4);
        }

        .demerit-text {
            font-weight: 600;
            color: #e2e8f0;
            font-size: 14px;
            line-height: 1.3;
            user-select: none;
            flex: 1;
        }

        .demerit-item.checked .demerit-text {
            color: #fed7d7;
            font-weight: 700;
        }

        .demerit-item.auto-failure .demerit-text {
            color: #fed7d7;
            font-weight: 700;
        }

        .btn-primary {
            background: linear-gradient(145deg, #3182ce, #2c5282);
            color: #e2e8f0;
            padding: 14px 25px;
            border: none;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.4s ease;
            box-shadow: 0 4px 15px rgba(49, 130, 206, 0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(49, 130, 206, 0.6);
            background: linear-gradient(145deg, #63b3ed, #3182ce);
        }

        .score-display {
            text-align: center;
            margin: 20px 0;
            padding: 15px;
            border-radius: 15px;
            font-size: 1em;
            font-weight: 800;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            transition: all 0.3s ease;
            border: 2px solid;
        }

        .score-pass {
            background: linear-gradient(145deg, #38a169, #2f855a);
            color: #f0fff4;
            border-color: #68d391;
        }

        .score-fail {
            background: linear-gradient(145deg, #e53e3e, #c53030);
            color: #fed7d7;
            border-color: #f56565;
        }

        .inspection-card {
            background: linear-gradient(135deg, #2d3748, #1a202c);
            border: 2px solid #4a5568;
            border-radius: 15px;
            padding: 20px;
            margin: 15px 0;
            position: relative;
            padding-top: 50px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            transition: all 0.4s ease;
        }

        .inspection-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.4);
        }

        .inspection-card.pass {
            border-color: #38a169;
            background: linear-gradient(135deg, #1a2f23, #22543d);
        }

        .inspection-card.fail {
            border-color: #e53e3e;
            background: linear-gradient(135deg, #2d1b1b, #3c1a1a);
        }

        .inspection-card h3 {
            font-size: 1.1em;
            margin-bottom: 8px;
            color: #e2e8f0;
        }

        .inspection-card p {
            font-size: 0.85em;
            margin-bottom: 5px;
            color: #cbd5e0;
        }

        .card-actions {
            position: absolute;
            top: 10px;
            right: 10px;
            display: flex;
            gap: 8px;
        }

        .edit-btn, .delete-btn {
            padding: 8px 12px;
            border: none;
            border-radius: 15px;
            cursor: pointer;
            font-weight: 700;
            font-size: 11px;
            transition: all 0.4s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .edit-btn {
            background: linear-gradient(145deg, #ed8936, #dd6b20);
            color: #fffaf0;
            box-shadow: 0 3px 10px rgba(237, 137, 54, 0.4);
        }

        .edit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(237, 137, 54, 0.6);
        }

        .delete-btn {
            background: linear-gradient(145deg, #e53e3e, #c53030);
            color: #fed7d7;
            box-shadow: 0 3px 10px rgba(229, 62, 62, 0.4);
        }

        .delete-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(229, 62, 62, 0.6);
        }

        .delete-all-btn {
            background: linear-gradient(145deg, #e53e3e, #c53030);
            color: #fed7d7;
            padding: 10px 15px;
            border: none;
            border-radius: 15px;
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 3px 10px rgba(229, 62, 62, 0.4);
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 11px;
        }

        .delete-all-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 5px 15px rgba(229, 62, 62, 0.6);
        }

        .export-btn {
            padding: 12px 20px;
            border: none;
            border-radius: 15px;
            font-size: 0.9em;
            font-weight: 600;
            cursor: pointer;
            margin: 8px;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
        }

        .export-btn:hover {
            transform: translateY(-2px);
        }

        .print-btn {
            background: linear-gradient(145deg, #805ad5, #6b46c1);
            color: #e9d8fd;
            box-shadow: 0 4px 15px rgba(128, 90, 213, 0.3);
        }

        .csv-btn {
            background: linear-gradient(145deg, #38a169, #2f855a);
            color: #f0fff4;
            box-shadow: 0 4px 15px rgba(56, 161, 105, 0.3);
        }

        .excel-btn {
            background: linear-gradient(145deg, #3182ce, #2c5282);
            color: #e2e8f0;
            box-shadow: 0 4px 15px rgba(49, 130, 206, 0.3);
        }

        h2 {
            font-size: 1.3em;
            margin-bottom: 15px;
            color: #e2e8f0;
        }

        h3 {
            font-size: 1.1em;
            margin-bottom: 10px;
            color: #e2e8f0;
        }

        h4 {
            font-size: 1em;
            margin-bottom: 10px;
            color: #e2e8f0;
        }

        p {
            color: #cbd5e0;
        }

        hr {
            border-color: #4a5568 !important;
        }

        @media (max-width: 768px) {
            body {
                padding: 5px;
            }
            
            .container {
                border-radius: 10px;
            }
            
            .header {
                padding: 15px;
            }
            
            .header h1 {
                font-size: 1.5em;
                margin-bottom: 5px;
            }
            
            .header p {
                font-size: 0.8em;
            }
            
            .main-content {
                padding: 15px;
            }
            
            .nav-buttons {
                gap: 5px;
                margin-bottom: 20px;
            }
            
            .nav-btn {
                padding: 10px 12px;
                font-size: 0.7em;
                border-radius: 15px;
                flex: 1;
                min-width: 0;
            }
            
            .export-btn {
                padding: 10px 15px;
                font-size: 0.8em;
                margin: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏢 Dorm Inspection System</h1>
            <p>Weekly Room Inspection Management</p>
        </div>
        
        <div class="main-content">
            <div class="nav-buttons">
                <button class="nav-btn active" onclick="showTab('new')">New Inspection</button>
                <button class="nav-btn" onclick="showTab('schedule')">Weekly Schedule</button>
                <button class="nav-btn" onclick="showTab('view')">View Inspections</button>
                <button class="nav-btn" onclick="showTab('reports')">Generate Report</button>
            </div>

            <!-- New Inspection Section -->
            <div id="new-section" class="section active">
                <h2 id="form-title">📋 New Room Inspection</h2>
                
                <div class="room-mode-toggle">
                    <div class="toggle-content">
                        <span class="toggle-label">Room Selection Mode:</span>
                        <label class="radio-option">
                            <input type="radio" name="roomMode" value="scheduled" checked onchange="toggleRoomMode()">
                            <span>📅 Scheduled Rooms Only</span>
                        </label>
                        <label class="radio-option">
                            <input type="radio" name="roomMode" value="all" onchange="toggleRoomMode()">
                            <span>🏢 All Available Rooms</span>
                        </label>
                    </div>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="roomNumber">Room Number *</label>
                        <select id="roomNumber" required>
                            <option value="">Select Room Number</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="inspectorName">Inspector Name (not required)</label>
                        <input type="text" id="inspectorName">
                    </div>
                </div>

                <div class="demerits-section">
                    <h3>Inspection Checklist (Click violations found)</h3>
                    <p style="margin-bottom: 20px; color: #a0aec0; font-size: 0.9em;">Regular violations are worth 1 point each. Auto-failure violations are worth 4 points each. 4 or more points = FAILED inspection</p>
                    
                    <h4 style="margin-bottom: 15px; color: #e2e8f0; font-size: 1em;">Regular Demerits (1 point each)</h4>
                    <div class="demerits-grid" id="demerits-container">
                        <!-- Populated by JavaScript -->
                    </div>
                    
                    <hr style="margin: 25px 0; border: none; border-top: 2px solid #4a5568;">
                    
                    <h4 style="margin-bottom: 15px; color: #fed7d7; font-weight: bold; font-size: 1em;">⚠️ Auto Failure Demerits (4 points each)</h4>
                    <div class="demerits-grid" id="auto-failure-container">
                        <!-- Populated by JavaScript -->
                    </div>
                </div>

                <div class="score-display score-pass" id="score-display">
                    Score: 0 points - PASSED
                </div>

                <div class="form-group">
                    <label for="notes">Additional Notes</label>
                    <textarea id="notes" rows="3" placeholder="Any additional observations or comments..."></textarea>
                </div>

                <button type="button" class="btn-primary" onclick="saveInspection()">Save Inspection</button>
                <button type="button" class="btn-primary" id="cancel-btn" style="display: none; margin-left: 15px; background: linear-gradient(145deg, #e53e3e, #c53030);" onclick="cancelEdit()">Cancel Edit</button>
            </div>

            <!-- Weekly Schedule Section -->
            <div id="schedule-section" class="section">
                <h2>📅 Weekly Inspection Schedule</h2>
                
                <div class="demerits-section">
                    <h3 style="margin-bottom: 20px; color: #e2e8f0; font-size: 1.2em;">Set Rooms for This Week's Inspections</h3>
                    
                    <div class="form-group">
                        <label for="roomInput">Add Room Numbers (Enter one at a time)</label>
                        <div style="display: flex; gap: 10px; align-items: flex-end;">
                            <div style="flex: 1;">
                                <select id="roomInput" style="margin-bottom: 0;">
                                    <option value="">Select Room to Add</option>
                                </select>
                            </div>
                            <button type="button" onclick="addRoomToSchedule()" style="background: linear-gradient(145deg, #38a169, #2f855a); color: #f0fff4; padding: 12px 20px; border: none; border-radius: 10px; font-weight: 600; cursor: pointer; box-shadow: 0 3px 10px rgba(56, 161, 105, 0.3); white-space: nowrap; font-size: 0.85em;">➕ Add Room</button>
                        </div>
                    </div>

                    <div style="margin: 25px 0;">
                        <h4 style="margin-bottom: 12px; color: #e2e8f0;">Scheduled Rooms for This Week:</h4>
                        <div id="scheduled-rooms" style="min-height: 50px; background: linear-gradient(135deg, #4a5568, #2d3748); border: 2px solid #3182ce; border-radius: 10px; padding: 15px; box-shadow: 0 3px 10px rgba(0, 0, 0, 0.3);">
                            <p style="color: #a0aec0; text-align: center; margin: 15px 0;">No rooms scheduled yet. Add rooms above.</p>
                        </div>
                    </div>

                    <div style="text-align: center; margin-top: 20px;">
                        <button type="button" onclick="clearAllScheduledRooms()" style="background: linear-gradient(145deg, #e53e3e, #c53030); color: #fed7d7; padding: 12px 20px; border: none; border-radius: 10px; font-weight: 600; cursor: pointer; box-shadow: 0 3px 10px rgba(229, 62, 62, 0.3); margin-right: 10px; font-size: 0.85em;">🗑️ Clear All</button>
                        <button type="button" onclick="saveSchedule()" style="background: linear-gradient(145deg, #3182ce, #2c5282); color: #e2e8f0; padding: 12px 20px; border: none; border-radius: 10px; font-weight: 600; cursor: pointer; box-shadow: 0 3px 10px rgba(49, 130, 206, 0.3); font-size: 0.85em;">💾 Save Schedule</button>
                    </div>
                </div>
            </div>

            <!-- View Inspections Section -->
            <div id="view-section" class="section">
                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; flex-wrap: wrap; gap: 15px;">
                    <h2 style="margin: 0; color: #e2e8f0;">📊 Recent Inspections</h2>
                    <button class="delete-all-btn" onclick="deleteAllInspections()">🗑️ Delete All Inspections</button>
                </div>
                <div id="inspections-list">
                    <!-- Populated by JavaScript -->
                </div>
            </div>

            <!-- Reports Section -->
            <div id="reports-section" class="section">
                <h2>📈 Inspection Report</h2>
                <div id="report-content">
                    <!-- Populated by JavaScript -->
                </div>
            </div>
        </div>
    </div>

    <script>
        // Global variables with proper initialization
        var inspections = [];
        var editingId = null;
        var scheduledRooms = [];
        
        var demerits = [
            "Bed not made or missing 341",
            "Mirror", 
            "Vanity/Sink",
            "Dirty tile or Carpet",
            "Foul odor",
            "High dust or excessive clutter",
            "Trash",
            "Fridge, freezer, or microwave",
            "Shower curtain",
            "Bathtub/shower",
            "Excessive mold build-up",
            "Toilet",
            "Dirty bathroom tile, rugs, or towels"
        ];

        var autoFailureDemerits = [
            "HAZMAT",
            "Unsecured wall locker or keys",
            "Unsecured valuables or uniforms",
            "Unsecured prescription medications",
            "Unsecured Tobacco",
            "Unsecured perishable food",
            "Contraband",
            "Safety items/open window",
            "To-go container/pizza box"
        ];

        // Data management functions
        function loadData() {
            try {
                var saved = localStorage.getItem('dormInspections');
                if (saved) {
                    inspections = JSON.parse(saved);
                }
                var savedSchedule = localStorage.getItem('weeklySchedule');
                if (savedSchedule) {
                    scheduledRooms = JSON.parse(savedSchedule);
                }
            } catch (e) {
                inspections = [];
                scheduledRooms = [];
            }
        }

        function saveData() {
            localStorage.setItem('dormInspections', JSON.stringify(inspections));
            localStorage.setItem('weeklySchedule', JSON.stringify(scheduledRooms));
        }

        // Room mode toggle function
        function toggleRoomMode() {
            updateRoomDropdown();
        }

        // Room dropdown update function
        function updateRoomDropdown() {
            var roomSelect = document.getElementById('roomNumber');
            var scheduledMode = document.querySelector('input[name="roomMode"]:checked').value === 'scheduled';
            
            roomSelect.innerHTML = '<option value="">Select Room Number</option>';
            
            if (scheduledMode) {
                if (scheduledRooms.length > 0) {
                    scheduledRooms.sort(function(a, b) { return parseInt(a) - parseInt(b); });
                    for (var i = 0; i < scheduledRooms.length; i++) {
                        var option = document.createElement('option');
                        option.value = scheduledRooms[i];
                        option.textContent = 'Room ' + scheduledRooms[i];
                        roomSelect.appendChild(option);
                    }
                } else {
                    var option = document.createElement('option');
                    option.value = '';
                    option.textContent = 'No rooms scheduled - Set up weekly schedule first';
                    option.disabled = true;
                    roomSelect.appendChild(option);
                }
            } else {
                var group200 = document.createElement('optgroup');
                group200.label = '200 Series';
                for (var i = 201; i <= 290; i++) {
                    var option = document.createElement('option');
                    option.value = i;
                    option.textContent = i;
                    group200.appendChild(option);
                }
                roomSelect.appendChild(group200);

                var group300 = document.createElement('optgroup');
                group300.label = '300 Series';
                for (var i = 301; i <= 390; i++) {
                    var option = document.createElement('option');
                    option.value = i;
                    option.textContent = i;
                    group300.appendChild(option);
                }
                roomSelect.appendChild(group300);
            }
        }

        // Setup form function
        function setupForm() {
            updateRoomDropdown();

            // Setup room input for scheduling
            var roomInput = document.getElementById('roomInput');
            if (roomInput) {
                roomInput.innerHTML = '<option value="">Select Room to Add</option>';
                
                var group200 = document.createElement('optgroup');
                group200.label = '200 Series';
                for (var i = 201; i <= 290; i++) {
                    var option = document.createElement('option');
                    option.value = i;
                    option.textContent = i;
                    group200.appendChild(option);
                }
                roomInput.appendChild(group200);

                var group300 = document.createElement('optgroup');
                group300.label = '300 Series';
                for (var i = 301; i <= 390; i++) {
                    var option = document.createElement('option');
                    option.value = i;
                    option.textContent = i;
                    group300.appendChild(option);
                }
                roomInput.appendChild(group300);
            }

            updateScheduledRoomsDisplay();

            // Setup regular demerits
            var container = document.getElementById('demerits-container');
            if (container) {
                container.innerHTML = '';
                
                for (var i = 0; i < demerits.length; i++) {
                    var card = document.createElement('div');
                    card.className = 'demerit-item';
                    card.setAttribute('data-index', i);
                    card.setAttribute('data-type', 'regular');
                    
                    var textDiv = document.createElement('div');
                    textDiv.className = 'demerit-text';
                    textDiv.textContent = demerits[i];
                    
                    card.appendChild(textDiv);
                    card.onclick = function() {
                        this.classList.toggle('checked');
                        updateScore();
                    };
                    
                    container.appendChild(card);
                }
            }

            // Setup auto-failure demerits
            var autoContainer = document.getElementById('auto-failure-container');
            if (autoContainer) {
                autoContainer.innerHTML = '';
                
                for (var i = 0; i < autoFailureDemerits.length; i++) {
                    var card = document.createElement('div');
                    card.className = 'demerit-item auto-failure';
                    card.setAttribute('data-index', i);
                    card.setAttribute('data-type', 'auto-failure');
                    
                    var textDiv = document.createElement('div');
                    textDiv.className = 'demerit-text';
                    textDiv.textContent = autoFailureDemerits[i];
                    
                    card.appendChild(textDiv);
                    card.onclick = function() {
                        this.classList.toggle('checked');
                        updateScore();
                    };
                    
                    autoContainer.appendChild(card);
                }
            }

            updateScore();
        }

        // Score update function
        function updateScore() {
            var regularChecked = document.querySelectorAll('.demerit-item[data-type="regular"].checked');
            var autoFailureChecked = document.querySelectorAll('.demerit-item[data-type="auto-failure"].checked');
            
            var regularScore = regularChecked.length;
            var autoFailureScore = autoFailureChecked.length * 4;
            var totalScore = regularScore + autoFailureScore;
            
            var status = totalScore >= 4 ? 'FAILED' : 'PASSED';
            
            var scoreDisplay = document.getElementById('score-display');
            var scoreText = 'Score: ' + totalScore + ' points';
            if (autoFailureScore > 0) {
                scoreText += ' (' + regularScore + ' regular + ' + autoFailureScore + ' auto-failure)';
            }
            scoreText += ' - ' + status;
            
            scoreDisplay.textContent = scoreText;
            scoreDisplay.className = 'score-display ' + (totalScore >= 4 ? 'score-fail' : 'score-pass');
        }

        // Tab navigation function
        function showTab(tab) {
            var sections = document.querySelectorAll('.section');
            for (var i = 0; i < sections.length; i++) {
                sections[i].classList.remove('active');
            }
            
            var navBtns = document.querySelectorAll('.nav-btn');
            for (var i = 0; i < navBtns.length; i++) {
                navBtns[i].classList.remove('active');
            }
            
            if (tab === 'new') {
                document.getElementById('new-section').classList.add('active');
                navBtns[0].classList.add('active');
                setupForm();
            } else if (tab === 'schedule') {
                document.getElementById('schedule-section').classList.add('active');
                navBtns[1].classList.add('active');
                setupForm();
            } else if (tab === 'view') {
                document.getElementById('view-section').classList.add('active');
                navBtns[2].classList.add('active');
                loadInspectionsList();
            } else if (tab === 'reports') {
                document.getElementById('reports-section').classList.add('active');
                navBtns[3].classList.add('active');
                generateReport();
            }
        }

        // Schedule management functions
        function addRoomToSchedule() {
            var roomInput = document.getElementById('roomInput');
            var roomNumber = roomInput.value;
            
            if (!roomNumber) {
                console.log('No room number selected');
                return;
            }
            
            var roomNumberStr = String(roomNumber);
            
            if (scheduledRooms.indexOf(roomNumberStr) !== -1) {
                console.log('Room ' + roomNumberStr + ' is already scheduled');
                return;
            }
            
            scheduledRooms.push(roomNumberStr);
            console.log('Added room to schedule:', roomNumberStr);
            
            roomInput.value = '';
            updateScheduledRoomsDisplay();
            updateRoomDropdown();
        }

        function removeRoomFromSchedule(roomNumber) {
            var index = scheduledRooms.indexOf(String(roomNumber));
            if (index > -1) {
                scheduledRooms.splice(index, 1);
                console.log('Removed room from schedule:', roomNumber);
                updateScheduledRoomsDisplay();
                updateRoomDropdown();
            }
        }

        function clearAllScheduledRooms() {
            scheduledRooms = [];
            console.log('Cleared all scheduled rooms');
            updateScheduledRoomsDisplay();
            updateRoomDropdown();
        }

        function saveSchedule() {
            saveData();
            console.log('Weekly schedule saved successfully');
        }

        function updateScheduledRoomsDisplay() {
            var container = document.getElementById('scheduled-rooms');
            
            if (scheduledRooms.length === 0) {
                container.innerHTML = '<p style="color: #a0aec0; text-align: center; margin: 15px 0;">No rooms scheduled yet. Add rooms above.</p>';
                return;
            }
            
            var sortedRooms = scheduledRooms.slice().sort(function(a, b) { return parseInt(a) - parseInt(b); });
            var html = '<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 8px;">';
            
            for (var i = 0; i < sortedRooms.length; i++) {
                var room = sortedRooms[i];
                html += '<div style="background: linear-gradient(145deg, #2d3748, #1a202c); border: 2px solid #3182ce; border-radius: 8px; padding: 10px; text-align: center; position: relative; box-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);">';
                html += '<button onclick="removeRoomFromSchedule(\'' + room + '\')" style="position: absolute; top: 3px; right: 3px; background: #e53e3e; color: #fed7d7; border: none; border-radius: 50%; width: 16px; height: 16px; font-size: 10px; cursor: pointer; display: flex; align-items: center; justify-content: center;">×</button>';
                html += '<div style="font-weight: 700; color: #90cdf4; font-size: 0.9em;">Room ' + room + '</div>';
                html += '</div>';
            }
            
            html += '</div>';
            html += '<p style="text-align: center; margin-top: 12px; color: #a0aec0; font-size: 0.8em;">Total: ' + scheduledRooms.length + ' rooms scheduled</p>';
            container.innerHTML = html;
        }

        // Inspection management functions
        function saveInspection() {
            var roomNumber = document.getElementById('roomNumber').value;
            var inspectorName = document.getElementById('inspectorName').value;
            var notes = document.getElementById('notes').value;
            
            var currentDate = new Date();
            var inspectionDate = currentDate.toISOString().split('T')[0];
            
            if (!roomNumber) {
                console.log('Form validation failed - missing required fields');
                showMessage('Please select a room number', 'error');
                return;
            }
            
            var selectedDemerits = [];
            var selectedAutoFailure = [];
            var regularChecked = document.querySelectorAll('.demerit-item[data-type="regular"].checked');
            var autoFailureChecked = document.querySelectorAll('.demerit-item[data-type="auto-failure"].checked');
            
            for (var i = 0; i < regularChecked.length; i++) {
                var index = parseInt(regularChecked[i].getAttribute('data-index'));
                selectedDemerits.push(demerits[index]);
            }
            
            for (var i = 0; i < autoFailureChecked.length; i++) {
                var index = parseInt(autoFailureChecked[i].getAttribute('data-index'));
                selectedAutoFailure.push(autoFailureDemerits[index]);
            }
            
            var regularScore = selectedDemerits.length;
            var autoFailureScore = selectedAutoFailure.length * 4;
            var totalScore = regularScore + autoFailureScore;
            
            var inspection = {
                id: editingId || Date.now(),
                roomNumber: roomNumber,
                inspectionDate: inspectionDate,
                inspectorName: inspectorName,
                demerits: selectedDemerits,
                autoFailureDemerits: selectedAutoFailure,
                regularScore: regularScore,
                autoFailureScore: autoFailureScore,
                score: totalScore,
                status: totalScore >= 4 ? 'FAILED' : 'PASSED',
                notes: notes,
                timestamp: new Date().toISOString()
            };
            
            var wasRoomRemoved = false;
            
            if (editingId) {
                for (var i = 0; i < inspections.length; i++) {
                    if (inspections[i].id === editingId) {
                        inspections[i] = inspection;
                        break;
                    }
                }
                console.log('Inspection updated successfully');
                showMessage('Inspection updated successfully!', 'success');
            } else {
                var roomNumberStr = String(roomNumber);
                var roomIndex = scheduledRooms.indexOf(roomNumberStr);
                
                console.log('Room to inspect:', roomNumberStr);
                console.log('Scheduled rooms:', scheduledRooms);
                console.log('Room index found:', roomIndex);
                
                if (roomIndex > -1) {
                    scheduledRooms.splice(roomIndex, 1);
                    wasRoomRemoved = true;
                    console.log('Room removed! New scheduled rooms:', scheduledRooms);
                    updateScheduledRoomsDisplay();
                    updateRoomDropdown();
                }
                
                inspections.push(inspection);
                
                if (wasRoomRemoved) {
                    showMessage('Inspection saved! Room ' + roomNumber + ' removed from schedule', 'success');
                } else {
                    showMessage('Inspection saved successfully!', 'success');
                }
            }
            
            saveData();
            resetForm();
        }

        function showMessage(text, type) {
            var scoreDisplay = document.getElementById('score-display');
            var originalText = scoreDisplay.textContent;
            var originalClass = scoreDisplay.className;
            
            scoreDisplay.textContent = text;
            if (type === 'error') {
                scoreDisplay.style.background = 'linear-gradient(145deg, #e53e3e, #c53030)';
                scoreDisplay.style.color = '#fed7d7';
            } else {
                scoreDisplay.style.background = 'linear-gradient(145deg, #38a169, #2f855a)';
                scoreDisplay.style.color = '#f0fff4';
            }
            
            setTimeout(function() {
                scoreDisplay.textContent = originalText;
                scoreDisplay.className = originalClass;
                scoreDisplay.style.background = '';
                scoreDisplay.style.color = '';
            }, 3000);
        }

        function resetForm() {
            editingId = null;
            document.getElementById('form-title').textContent = 'New Room Inspection';
            document.getElementById('cancel-btn').style.display = 'none';
            document.getElementById('roomNumber').value = '';
            document.getElementById('inspectorName').value = '';
            document.getElementById('notes').value = '';
            
            var allCards = document.querySelectorAll('.demerit-item');
            for (var i = 0; i < allCards.length; i++) {
                allCards[i].classList.remove('checked');
            }
            updateScore();
        }

        function editInspection(id) {
            var inspection = null;
            for (var i = 0; i < inspections.length; i++) {
                if (inspections[i].id === id) {
                    inspection = inspections[i];
                    break;
                }
            }
            
            if (!inspection) return;
            
            editingId = id;
            document.getElementById('form-title').textContent = 'Edit Room Inspection';
            document.getElementById('cancel-btn').style.display = 'inline-block';
            
            document.getElementById('roomNumber').value = inspection.roomNumber;
            document.getElementById('inspectorName').value = inspection.inspectorName;
            document.getElementById('notes').value = inspection.notes;
            
            var allCards = document.querySelectorAll('.demerit-item');
            for (var i = 0; i < allCards.length; i++) {
                allCards[i].classList.remove('checked');
            }
            
            if (inspection.demerits) {
                for (var i = 0; i < inspection.demerits.length; i++) {
                    var demeritIndex = demerits.indexOf(inspection.demerits[i]);
                    if (demeritIndex !== -1) {
                        var card = document.querySelector('[data-type="regular"][data-index="' + demeritIndex + '"]');
                        if (card) {
                            card.classList.add('checked');
                        }
                    }
                }
            }
            
            if (inspection.autoFailureDemerits) {
                for (var i = 0; i < inspection.autoFailureDemerits.length; i++) {
                    var demeritIndex = autoFailureDemerits.indexOf(inspection.autoFailureDemerits[i]);
                    if (demeritIndex !== -1) {
                        var card = document.querySelector('[data-type="auto-failure"][data-index="' + demeritIndex + '"]');
                        if (card) {
                            card.classList.add('checked');
                        }
                    }
                }
            }
            
            updateScore();
            showTab('new');
        }

        function cancelEdit() {
            resetForm();
        }

        function deleteInspection(id) {
            for (var i = 0; i < inspections.length; i++) {
                if (inspections[i].id === id) {
                    inspections.splice(i, 1);
                    break;
                }
            }
            
            saveData();
            loadInspectionsList();
            console.log('Inspection deleted successfully');
        }

        function deleteAllInspections() {
            if (inspections.length === 0) {
                console.log('No inspections to delete');
                return;
            }
            
            var button = event.target;
            var clickCount = parseInt(button.getAttribute('data-click-count') || '0');
            
            clickCount++;
            button.setAttribute('data-click-count', clickCount);
            
            var originalText = 'Delete All Inspections';
            
            if (clickCount === 1) {
                button.textContent = 'Click 2 More Times (' + inspections.length + ' records)';
                button.style.background = 'linear-gradient(145deg, #ed8936, #dd6b20)';
                
                setTimeout(function() {
                    button.setAttribute('data-click-count', '0');
                    button.textContent = originalText;
                    button.style.background = 'linear-gradient(145deg, #e53e3e, #c53030)';
                }, 5000);
                
            } else if (clickCount === 2) {
                button.textContent = 'FINAL WARNING - Click Once More';
                button.style.background = 'linear-gradient(145deg, #c53030, #9b2c2c)';
                
            } else if (clickCount >= 3) {
                inspections = [];
                saveData();
                loadInspectionsList();
                
                button.setAttribute('data-click-count', '0');
                button.textContent = originalText;
                button.style.background = 'linear-gradient(145deg, #e53e3e, #c53030)';
                
                console.log('All inspections deleted successfully');
            }
        }

        function loadInspectionsList() {
            var list = document.getElementById('inspections-list');
            
            if (inspections.length === 0) {
                list.innerHTML = '<p style="text-align: center; color: #a0aec0; padding: 30px; font-size: 1em;">No inspections recorded yet.</p>';
                return;
            }
            
            var html = '';
            var sortedInspections = inspections.slice().sort(function(a, b) {
                return new Date(b.inspectionDate) - new Date(a.inspectionDate);
            });
            
            for (var i = 0; i < sortedInspections.length; i++) {
                var inspection = sortedInspections[i];
                
                html += '<div class="inspection-card ' + inspection.status.toLowerCase() + '">';
                html += '<div class="card-actions">';
                html += '<button class="edit-btn" onclick="editInspection(' + inspection.id + ')">Edit</button>';
                html += '<button class="delete-btn" onclick="deleteInspection(' + inspection.id + ')">Delete</button>';
                html += '</div>';
                html += '<h3>Room ' + inspection.roomNumber + ' - ' + inspection.status + '</h3>';
                html += '<p><strong>Date:</strong> ' + new Date(inspection.inspectionDate).toLocaleDateString() + '</p>';
                html += '<p><strong>Inspector:</strong> ' + inspection.inspectorName + '</p>';
                html += '<p><strong>Score:</strong> ' + inspection.score + ' points';
                if (inspection.autoFailureScore && inspection.autoFailureScore > 0) {
                    html += ' (' + (inspection.regularScore || 0) + ' regular + ' + inspection.autoFailureScore + ' auto-failure)';
                }
                html += '</p>';
                
                if (inspection.demerits && inspection.demerits.length > 0) {
                    html += '<p><strong>Regular Violations:</strong> ' + inspection.demerits.join(', ') + '</p>';
                }
                
                if (inspection.autoFailureDemerits && inspection.autoFailureDemerits.length > 0) {
                    html += '<p><strong>Auto-Failure Violations:</strong> ' + inspection.autoFailureDemerits.join(', ') + '</p>';
                }
                
                if (inspection.notes) {
                    html += '<p><strong>Notes:</strong> ' + inspection.notes + '</p>';
                }
                
                html += '</div>';
            }
            
            list.innerHTML = html;
        }

        function generateReport() {
            var content = document.getElementById('report-content');
            
            if (inspections.length === 0) {
                content.innerHTML = '<p style="font-size: 1em; color: #a0aec0;">No data available for report generation.</p>';
                return;
            }
            
            var total = inspections.length;
            var passed = inspections.filter(function(i) { return i.status === 'PASSED'; }).length;
            var failed = inspections.filter(function(i) { return i.status === 'FAILED'; }).length;
            var passRate = ((passed / total) * 100).toFixed(1);
            
            var html = '<div style="text-align: center; margin-bottom: 30px;">';
            html += '<h3 style="font-size: 1.5em; margin-bottom: 8px; color: #e2e8f0;">Weekly Dorm Inspection Summary</h3>';
            html += '<p style="font-size: 1em; color: #a0aec0;">Report Generated: ' + new Date().toLocaleDateString() + '</p>';
            html += '</div>';
            
            html += '<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 15px; margin: 20px 0;">';
            html += '<div style="background: linear-gradient(145deg, #2d3748, #1a202c); padding: 20px; border-radius: 15px; text-align: center; box-shadow: 0 4px 15px rgba(0,0,0,0.3); border: 2px solid #4a5568;">';
            html += '<div style="font-size: 2em; font-weight: bold; color: #e2e8f0;">' + total + '</div>';
            html += '<div style="color: #cbd5e0; font-size: 0.9em; font-weight: 600;">Total</div>';
            html += '</div>';
            html += '<div style="background: linear-gradient(145deg, #2d3748, #1a202c); padding: 20px; border-radius: 15px; text-align: center; box-shadow: 0 4px 15px rgba(0,0,0,0.3); border: 2px solid #38a169;">';
            html += '<div style="font-size: 2em; font-weight: bold; color: #68d391;">' + passed + '</div>';
            html += '<div style="color: #cbd5e0; font-size: 0.9em; font-weight: 600;">Passed</div>';
            html += '</div>';
            html += '<div style="background: linear-gradient(145deg, #2d3748, #1a202c); padding: 20px; border-radius: 15px; text-align: center; box-shadow: 0 4px 15px rgba(0,0,0,0.3); border: 2px solid #e53e3e;">';
            html += '<div style="font-size: 2em; font-weight: bold; color: #f56565;">' + failed + '</div>';
            html += '<div style="color: #cbd5e0; font-size: 0.9em; font-weight: 600;">Failed</div>';
            html += '</div>';
            html += '<div style="background: linear-gradient(145deg, #2d3748, #1a202c); padding: 20px; border-radius: 15px; text-align: center; box-shadow: 0 4px 15px rgba(0,0,0,0.3); border: 2px solid #3182ce;">';
            html += '<div style="font-size: 2em; font-weight: bold; color: #90cdf4;">' + passRate + '%</div>';
            html += '<div style="color: #cbd5e0; font-size: 0.9em; font-weight: 600;">Pass Rate</div>';
            html += '</div>';
            html += '</div>';
            
            // Generate most common demerits section
            var demeritCounts = {};
            
            // Count regular demerits
            for (var i = 0; i < inspections.length; i++) {
                var inspection = inspections[i];
                if (inspection.demerits) {
                    for (var j = 0; j < inspection.demerits.length; j++) {
                        var demerit = inspection.demerits[j];
                        demeritCounts[demerit] = (demeritCounts[demerit] || 0) + 1;
                    }
                }
                if (inspection.autoFailureDemerits) {
                    for (var j = 0; j < inspection.autoFailureDemerits.length; j++) {
                        var demerit = inspection.autoFailureDemerits[j];
                        demeritCounts[demerit] = (demeritCounts[demerit] || 0) + 1;
                    }
                }
            }
            
            // Convert to array and sort
            var sortedDemerits = [];
            for (var demerit in demeritCounts) {
                sortedDemerits.push({
                    name: demerit,
                    count: demeritCounts[demerit],
                    percentage: ((demeritCounts[demerit] / total) * 100).toFixed(1)
                });
            }
            sortedDemerits.sort(function(a, b) { return b.count - a.count; });
            
            // Top 10 most common demerits section
            if (sortedDemerits.length > 0) {
                html += '<div style="margin: 30px 0; background: linear-gradient(135deg, #2d3748, #1a202c); padding: 25px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.3); border: 2px solid #4a5568;">';
                html += '<h3 style="font-size: 1.3em; margin-bottom: 20px; color: #e2e8f0; text-align: center;">Top 10 Most Common Violations</h3>';
                
                var topDemerits = sortedDemerits.slice(0, 10);
                for (var i = 0; i < topDemerits.length; i++) {
                    var demerit = topDemerits[i];
                    var isAutoFailure = autoFailureDemerits.indexOf(demerit.name) !== -1;
                    var barColor = isAutoFailure ? '#f56565' : '#3182ce';
                    var textColor = isAutoFailure ? '#fed7d7' : '#e2e8f0';
                    var badgeText = isAutoFailure ? 'AUTO-FAIL' : 'REGULAR';
                    var badgeColor = isAutoFailure ? '#c53030' : '#2c5282';
                    
                    html += '<div style="margin-bottom: 15px; background: linear-gradient(135deg, #4a5568, #2d3748); padding: 15px; border-radius: 10px; border: 1px solid ' + barColor + ';">';
                    html += '<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">';
                    html += '<div style="flex: 1;">';
                    html += '<span style="font-weight: 600; color: ' + textColor + '; font-size: 0.9em;">' + (i + 1) + '. ' + demerit.name + '</span>';
                    html += '<span style="background: ' + badgeColor + '; color: white; padding: 2px 6px; border-radius: 12px; font-size: 0.7em; font-weight: 700; margin-left: 8px;">' + badgeText + '</span>';
                    html += '</div>';
                    html += '<div style="text-align: right; color: #cbd5e0; font-size: 0.85em; font-weight: 600;">';
                    html += demerit.count + ' times (' + demerit.percentage + '%)';
                    html += '</div>';
                    html += '</div>';
                    
                    // Progress bar
                    var maxCount = topDemerits[0].count;
                    var barWidth = (demerit.count / maxCount) * 100;
                    html += '<div style="background: rgba(74, 85, 104, 0.3); height: 6px; border-radius: 3px; overflow: hidden;">';
                    html += '<div style="background: ' + barColor + '; height: 100%; width: ' + barWidth + '%; border-radius: 3px; transition: width 0.3s ease;"></div>';
                    html += '</div>';
                    html += '</div>';
                }
                html += '</div>';
            }
            
            html += '<div style="text-align: center; margin: 30px 0;">';
            html += '<button onclick="window.print()" class="export-btn print-btn">Print Report</button>';
            html += '<button onclick="exportToCSV()" class="export-btn csv-btn">Export CSV</button>';
            html += '<button onclick="exportToExcel()" class="export-btn excel-btn">Export Excel</button>';
            html += '</div>';
            
            content.innerHTML = html;
        }

        function exportToCSV() {
            if (inspections.length === 0) {
                console.log('No data to export');
                return;
            }
            
            var headers = ['Room Number', 'Date', 'Inspector', 'Regular Score', 'Auto-Failure Score', 'Total Score', 'Status', 'Notes'];
            var csvContent = headers.join(',') + '\n';
            
            for (var i = 0; i < inspections.length; i++) {
                var inspection = inspections[i];
                var row = [
                    inspection.roomNumber,
                    inspection.inspectionDate,
                    inspection.inspectorName,
                    inspection.regularScore || 0,
                    inspection.autoFailureScore || 0,
                    inspection.score,
                    inspection.status,
                    '"' + (inspection.notes || '') + '"'
                ];
                csvContent += row.join(',') + '\n';
            }
            
            var blob = new Blob([csvContent], { type: 'text/csv' });
            var url = URL.createObjectURL(blob);
            var a = document.createElement('a');
            a.href = url;
            a.download = 'dorm-inspection-report-' + new Date().toISOString().split('T')[0] + '.csv';
            a.click();
            URL.revokeObjectURL(url);
            
            console.log('CSV report downloaded successfully');
        }

        function exportToExcel() {
            if (inspections.length === 0) {
                console.log('No data to export');
                return;
            }

            var total = inspections.length;
            var passed = inspections.filter(function(i) { return i.status === 'PASSED'; }).length;
            var failed = inspections.filter(function(i) { return i.status === 'FAILED'; }).length;
            var passRate = ((passed / total) * 100).toFixed(1);
            
            // Create compact HTML table optimized for 8.5x11 paper
            var htmlContent = '<!DOCTYPE html><html><head><meta charset="utf-8">';
            htmlContent += '<style>';
            htmlContent += '@page { size: 8.5in 11in; margin: 0.5in; }';
            htmlContent += 'body { font-family: Arial, sans-serif; font-size: 8pt; margin: 0; }';
            htmlContent += 'table { border-collapse: collapse; width: 100%; page-break-inside: avoid; }';
            htmlContent += 'th, td { border: 1px solid #000; padding: 1px; text-align: center; font-size: 7pt; }';
            htmlContent += '.title { background: #1a365d; color: white; font-size: 12pt; font-weight: bold; padding: 4px; }';
            htmlContent += '.summary { background: #e2e8f0; color: #1a202c; font-weight: bold; font-size: 7pt; padding: 2px; }';
            htmlContent += '.header-room { background: #2d3748; color: white; font-weight: bold; width: 40px; font-size: 6pt; }';
            htmlContent += '.header-date { background: #2d3748; color: white; font-weight: bold; width: 50px; font-size: 6pt; }';
            htmlContent += '.header-af { background: #c53030; color: white; font-weight: bold; width: 8px; font-size: 5pt; writing-mode: vertical-rl; padding: 2px 1px; height: 80px; }';
            htmlContent += '.header-regular { background: #3182ce; color: white; font-weight: bold; width: 8px; font-size: 5pt; writing-mode: vertical-rl; padding: 2px 1px; height: 80px; }';
            htmlContent += '.header-status { background: #2d3748; color: white; font-weight: bold; width: 35px; font-size: 6pt; }';
            htmlContent += '.header-notes { background: #2d3748; color: white; font-weight: bold; width: 80px; font-size: 6pt; }';
            htmlContent += '.room-cell { background: #f7fafc; font-weight: bold; font-size: 7pt; }';
            htmlContent += '.violation-af { background: #fed7d7; color: #c53030; font-weight: bold; font-size: 8pt; width: 8px; }';
            htmlContent += '.violation-regular { background: #bee3f8; color: #2c5282; font-weight: bold; font-size: 8pt; width: 8px; }';
            htmlContent += '.no-violation { background: #f0fff4; width: 8px; }';
            htmlContent += '.status-pass { background: #68d391; color: #1a202c; font-weight: bold; font-size: 6pt; }';
            htmlContent += '.status-fail { background: #f56565; color: white; font-weight: bold; font-size: 6pt; }';
            htmlContent += '.notes-cell { background: #f7fafc; font-size: 5pt; text-align: left; max-width: 80px; overflow: hidden; }';
            htmlContent += '</style></head><body>';
            
            // Compact title section
            htmlContent += '<table>';
            var totalCols = 4 + autoFailureDemerits.length + demerits.length; // Room + Date + AF + Regular + Status + Notes
            htmlContent += '<tr><td colspan="' + totalCols + '" class="title">DORM INSPECTION REPORT - ' + new Date().toLocaleDateString() + '</td></tr>';
            htmlContent += '</table><br>';
            
            // Compact summary in one row
            htmlContent += '<table>';
            htmlContent += '<tr>';
            htmlContent += '<td class="summary">Total: ' + total + '</td>';
            htmlContent += '<td class="summary" style="background: #c6f6d5;">Pass: ' + passed + '</td>';
            htmlContent += '<td class="summary" style="background: #fed7d7;">Fail: ' + failed + '</td>';
            htmlContent += '<td class="summary" style="background: #bee3f8;">Rate: ' + passRate + '%</td>';
            htmlContent += '</tr>';
            htmlContent += '</table><br>';
            
            // Main data table with single header row and angled text
            htmlContent += '<table>';
            
            // Single header row with angled text
            htmlContent += '<tr>';
            htmlContent += '<td class="header-room">RM</td>';
            htmlContent += '<td class="header-date">DATE</td>';
            
            // Auto-failure headers with 45-degree angle
            for (var i = 0; i < autoFailureDemerits.length; i++) {
                htmlContent += '<td style="background: #c53030; color: white; font-weight: bold; width: 20px; font-size: 5pt; border: 1px solid #000; padding: 2px; text-align: center; transform: rotate(-45deg); transform-origin: center; height: 60px; white-space: nowrap; overflow: hidden;">' + escapeHtml(autoFailureDemerits[i]) + '</td>';
            }
            
            // Regular headers with 45-degree angle
            for (var i = 0; i < demerits.length; i++) {
                htmlContent += '<td style="background: #3182ce; color: white; font-weight: bold; width: 20px; font-size: 5pt; border: 1px solid #000; padding: 2px; text-align: center; transform: rotate(-45deg); transform-origin: center; height: 60px; white-space: nowrap; overflow: hidden;">' + escapeHtml(demerits[i]) + '</td>';
            }
            
            htmlContent += '<td class="header-status">PASS/FAIL</td>';
            htmlContent += '<td class="header-notes">NOTES</td>';
            htmlContent += '</tr>';
            
            // Compact data rows
            for (var i = 0; i < inspections.length; i++) {
                var inspection = inspections[i];
                htmlContent += '<tr>';
                
                // Compact room and date
                htmlContent += '<td class="room-cell">' + escapeHtml(inspection.roomNumber) + '</td>';
                htmlContent += '<td class="room-cell">' + new Date(inspection.inspectionDate).toLocaleDateString('en-US', {month: 'numeric', day: 'numeric'}) + '</td>';
                
                // Auto-failure violations with matching width
                for (var j = 0; j < autoFailureDemerits.length; j++) {
                    var hasAutoFailure = inspection.autoFailureDemerits && inspection.autoFailureDemerits.indexOf(autoFailureDemerits[j]) !== -1;
                    htmlContent += '<td style="background: ' + (hasAutoFailure ? '#fed7d7' : '#f0fff4') + '; color: ' + (hasAutoFailure ? '#c53030' : '') + '; font-weight: bold; font-size: 8pt; width: 20px; border: 1px solid #000; text-align: center;">' + (hasAutoFailure ? 'X' : '') + '</td>';
                }
                
                // Regular violations with matching width
                for (var j = 0; j < demerits.length; j++) {
                    var hasDemerit = inspection.demerits && inspection.demerits.indexOf(demerits[j]) !== -1;
                    htmlContent += '<td style="background: ' + (hasDemerit ? '#bee3f8' : '#f0fff4') + '; color: ' + (hasDemerit ? '#2c5282' : '') + '; font-weight: bold; font-size: 8pt; width: 20px; border: 1px solid #000; text-align: center;">' + (hasDemerit ? 'X' : '') + '</td>';
                }
                
                // Compact status
                var statusClass = inspection.status === 'PASSED' ? 'status-pass' : 'status-fail';
                var statusText = inspection.status === 'PASSED' ? 'PASS' : 'FAIL';
                htmlContent += '<td class="' + statusClass + '">' + statusText + '</td>';
                
                // Ultra-compact notes
                var notesText = inspection.notes || '';
                if (notesText.length > 40) {
                    notesText = notesText.substring(0, 37) + '...';
                }
                htmlContent += '<td class="notes-cell">' + escapeHtml(notesText) + '</td>';
                htmlContent += '</tr>';
            }
            
            htmlContent += '</table>';
            htmlContent += '</body></html>';
            
            var blob = new Blob([htmlContent], { type: 'application/vnd.ms-excel' });
            var url = URL.createObjectURL(blob);
            var a = document.createElement('a');
            a.href = url;
            a.download = 'dorm-inspection-report-' + new Date().toISOString().split('T')[0] + '.xls';
            a.click();
            URL.revokeObjectURL(url);
            
            console.log('Compact Excel report downloaded successfully');
        }

        function escapeHtml(unsafe) {
            if (unsafe === null || unsafe === undefined) return '';
            return String(unsafe)
                .replace(/&/g, "&amp;")
                .replace(/</g, "&lt;")
                .replace(/>/g, "&gt;")
                .replace(/"/g, "&quot;")
                .replace(/'/g, "&#039;");
        }

        function escapeXml(unsafe) {
            if (unsafe === null || unsafe === undefined) return '';
            return String(unsafe).replace(/[<>&'"]/g, function (c) {
                switch (c) {
                    case '<': return '&lt;';
                    case '>': return '&gt;';
                    case '&': return '&amp;';
                    case '\'': return '&apos;';
                    case '"': return '&quot;';
                }
            });
        }

        // Initialize the application
        window.onload = function() {
            loadData();
            setupForm();
        };
    </script>
</body>
</html>
